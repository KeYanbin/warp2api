# Warp API Configuration Example
# 复制此文件为 .env 并根据需要修改配置

# 服务器配置
HOST=0.0.0.0
PORT=8002

# Warp JWT Token (必须配置)
WARP_JWT=your_warp_jwt_token_here

# 超时配置（秒）
# 连接超时 - 建立连接的最大时间
WARP_CONNECT_TIMEOUT=30

# 读取超时 - 读取响应数据的最大时间（适合长SSE流）
# 如果经常出现 ReadTimeout，可以增加这个值到 600 (10分钟) 或更高
WARP_READ_TIMEOUT=300

# 写入超时 - 发送请求数据的最大时间
WARP_WRITE_TIMEOUT=60

# 连接池超时 - 从连接池获取连接的最大时间
WARP_POOL_TIMEOUT=60

# TLS 设置 (开发环境可以设为 true 跳过证书验证)
# WARP_INSECURE_TLS=false
